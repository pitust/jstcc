{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C Launch",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceRoot}/tmp",
            "stopAtEntry": false,
            "launchCompleteCommand": "exec-run",
            "MIMode": "gdb",
            "cwd": "${workspaceRoot}",
            "miDebuggerPath": "/usr/bin/gdb",
            "targetArchitecture": "x64"
        }
    ]
}